export type User = {
  id: string;
  description: string | null;
  createdAt: string;
  username: string;
  host: string | null;
  name: string;
  onlineStatus: string;
  avatarUrl: string;
  avatarBlurhash: string;
};
